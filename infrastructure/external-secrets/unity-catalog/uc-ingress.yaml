apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: unitycatalog-ui
  namespace: data-platform
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: uc.tpomaville.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: unity-catalog-unitycatalog-ui
            port:
              number: 3000